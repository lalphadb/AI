{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(cat | jq -r '.tool_input.file_path // empty'); if [[ \"$file_path\" == *.py ]]; then cd /home/lalpha/projets/ai-tools/ai-orchestrator && python3 -m py_compile \"$file_path\" 2>&1 && echo '✅ Syntaxe OK' || echo '❌ Erreur syntaxe'; fi",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/lalpha/projets/ai-tools/ai-orchestrator && docker compose ps --format '{{.Name}}: {{.Status}}' | grep -E 'orchestrator' | head -3",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
