{
  "description": "Optimisation de la base de données StudiosDB",
  "steps": [
    {
      "type": "command",
      "command": "mysqlcheck",
      "args": ["-o", "studiosdb"],
      "timeout": 60000
    },
    {
      "type": "command",
      "command": "mysql",
      "args": ["-e", "ANALYZE TABLE studiosdb.*;"],
      "timeout": 30000
    }
  ]
}
